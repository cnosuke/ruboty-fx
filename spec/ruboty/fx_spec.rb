require 'spec_helper'

describe Ruboty::Fx do
  it 'has a version number' do
    expect(Ruboty::Fx::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
